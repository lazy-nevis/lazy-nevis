{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay-capability",
  "description": "Minimal capabilities for rendering and dismissing an alert overlay",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "core:window:allow-hide"
  ]
}
