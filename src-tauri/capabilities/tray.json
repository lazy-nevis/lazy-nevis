{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray-capability",
  "description": "Minimal capabilities for the tray quick panel window",
  "windows": ["tray"],
  "permissions": [
    "core:default",
    "core:window:allow-hide"
  ]
}
