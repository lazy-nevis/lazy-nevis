# OpenSpec Changes

Create changes with the pinned CLI (`bunx openspec new change <change-id>`) or manually using the active schema. Each behavior change contains `proposal.md`, `design.md`, `tasks.md`, and deltas under `specs/<capability>/spec.md`. External contributors wait for proposal approval. Completed work is strictly validated and archived into `changes/archive/` with capability deltas merged.
