# Release Screenshot Set

Real screenshots must come from the tagged release candidate and contain no personal window titles, paths, or history. Capture at 1440x900 or higher and crop consistently:

- `dashboard-light.png` and `dashboard-dark.png`
- `history-light.png` and `history-dark.png`
- `settings-light.png` and `settings-dark.png`
- `notification-alert.png` and `fullscreen-alert.png`

Before publishing, switch to seeded non-personal demo data, capture each view on a supported OS, optimize without resizing text, and verify README links. Images are intentionally not fabricated before the RC UI is available.
