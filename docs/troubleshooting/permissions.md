# Permissions

macOS Accessibility is needed for active-window metadata. Open System Settings > Privacy & Security > Accessibility, enable the installed LazyNevis app, then restart it. Re-add the app after moving/replacing an unsigned build. Notification permission is optional and can be changed under Notifications. LazyNevis does not need Screen Recording or keystroke access; report prompts for unexpected permissions.
