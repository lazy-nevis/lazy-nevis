# Tray

Closing the main window normally keeps LazyNevis in the tray. Look in macOS menu extras, Windows notification-area overflow, or the Linux status-notifier area. Some Linux shells need an AppIndicator/status-notifier extension. If no icon appears, launch from a terminal, record desktop environment/display server/package format, and quit any duplicate process before retrying.
