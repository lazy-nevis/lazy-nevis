# Global Shortcuts

Another application or the OS may reserve a shortcut. Quit conflicting tools and restart LazyNevis after changing shortcuts. On macOS use Command; on Windows/Linux use Control. Stored shortcut customization is a known incomplete area until backend dynamic registration ships, so current builds may continue using documented defaults.
