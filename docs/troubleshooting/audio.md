# Audio

Confirm the selected output device and system volume, then test a built-in sound. Custom files must be readable MP3, WAV, or OGG files and may stop working if moved. Linux requires a working ALSA-compatible output stack. Overlay audio loops until dismissal; if playback persists after dismissal, collect the OS, package type, and logs for a bug report without including private paths.
